# 贡献指南

感谢你对「提示词工程案例库」的关注！欢迎通过以下方式参与贡献。

---

## 如何贡献

### 报告问题

- 使用 [GitHub Issues](../../issues) 提交 Bug 报告或内容建议
- 请描述清楚：当前内容 → 期望内容 → 为什么需要改进

### 提交内容

1. **Fork** 本仓库
2. 创建特性分支：`git checkout -b feature/your-feature`
3. 修改内容
4. 提交：`git commit -m "feat: 简短描述"`
5. 推送：`git push origin feature/your-feature`
6. 发起 **Pull Request**

---

## 内容规范

### 提示词模板格式

每个提示词模板应包含以下要素：

- **提示词标识**使用 `<small style="color:#888">提示词：</small>` 格式，缩小变灰，放在提示词内容上方
- **提示词内容**使用 `**加粗**` 格式，正常字号，视觉突出
- **说明性文字**（适用场景、填写说明、导航、示例等）使用 `<small style="color:#888">` 包裹，缩小变灰，不抢提示词注意力
- **各提示词区域**用 `---` 分隔
- **技术示例**（如 SQL、Python 代码等）仍使用代码块格式

```markdown
<small style="color:#888">提示词：</small>

**你是一位{角色描述}。**

**请{任务描述}。**

**约束：**
- {约束1}
- {约束2}

**输出格式：**
## {格式1}
## {格式2}
```

<small style="color:#888">适用场景：{一句话说明}</small>

<small style="color:#888">填写说明：{变量解释}（如有必要）</small>

### 变量标记

- 必填变量：`{变量名}`
- 可选参数：`[可选参数]`

### 交叉引用

引用项目内其他文件时使用相对路径（GitHub 兼容）：

```markdown
<!-- 从同目录文件引用（带编号前缀） -->
详见 [系统提示词案例](02-AI编程助手系统提示词案例.md)

<!-- 从不同目录文件引用 -->
详见 [核心技术手册](../01-入门与理论/02-提示词工程核心技术手册.md#三链式思维提示chain-of-thought-cot)

<!-- 从根目录文件引用 -->
详见 [核心技术手册](01-入门与理论/02-提示词工程核心技术手册.md)
```

**注意**：文件名中的编号前缀（如 `01-`、`02-`）表示同目录内的学习顺序，引用时必须包含。

引用外部资源时标注来源：

```markdown
数据来源：Wei et al., 2022, [Chain-of-Thought Prompting](https://arxiv.org/abs/2201.11903)
```

---

## 质量标准

### 新增提示词模板

- [ ] 包含 Role / Task / Constraints / Output Format 四字段
- [ ] 变量用 `{变量名}` 标记
- [ ] 说明适用场景
- [ ] 格式与同文件其他模板一致

### 新增技术章节

- [ ] 包含：原理 → 适用场景 → 模板 → 效果数据
- [ ] 效果数据标注来源（论文/基准/实践）
- [ ] 与现有章节无重复
- [ ] 更新技术速查表

### 修改现有内容

- [ ] 不破坏已有交叉引用
- [ ] 修改技术描述需与论文原文一致
- [ ] 修改效果数据需标注来源
- [ ] 更新相关文件中的关联内容

---

## 提交规范

### Commit 消息格式

```
<type>: <subject>

<body>
```

**type 类型**：

| 类型 | 说明 |
|------|------|
| feat | 新增内容（新模板、新技术、新文件） |
| fix | 修复错误（错别字、格式、链接） |
| update | 更新内容（更新数据、补充示例） |
| refactor | 重构（调整结构、合并内容） |
| docs | 项目文档修改 |

**示例**：
```
feat: 新增多轮对话提示词模板
fix: 修复核心技术手册中CoT章节的公式错误
update: 更新开源项目Star数至2026年5月
docs: 添加项目架构文档
```

---

## 内容来源要求

- **优先引用**：官方文档、学术论文、GitHub开源项目
- **标注出处**：论文引用格式：作者 + 年份 + 标题
- **时效性**：开源项目信息需标注数据获取时间
- **合规性**：不复制粘贴他人受版权保护的内容，改写后引用

---

## 审核流程

1. 提交 PR 后，维护者会在 7 天内审核
2. 审核重点：格式规范、内容质量、交叉引用正确性
3. 如需修改，维护者会在 PR 中留言
4. 审核通过后合并

---

## 行为准则

- 尊重所有贡献者
- 提供建设性的反馈
- 关注内容本身，而非个人
